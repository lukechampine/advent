=input split "," | concatmap (split "-" |< { enum (int x) (int y + 1) })
filter (digits | { cleave (len x / 2) x } | map flatten |< ==) | sum
filter (digits | cleaves | any -<{ replicate (len y / len x) x | flatten == y }) | sum
